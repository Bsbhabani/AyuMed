services:
  - type: web
    name: ayurmed
    runtime: node
    rootDir: .
    buildCommand: npm ci && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
